.venv/
__pycache__/
*.pyc
data/
runs/
*.safetensors
*.bin
data-code/train.jsonl
data-code/val.jsonl
data-hermes/

# Regenerated by `python -m typed_decisions.hole_data --out <dir>` (src/typed_decisions/hole_data.py).
# ~750 MB in the shared west checkout, derived from the superproject's own history,
# so it is rebuildable rather than archived (CLAUDE.md: large binaries go to DataLad
# + git-annex + B2, never straight into git). stats.json stays tracked on purpose.
data-holes/*.jsonl
data-holes-repo/*.jsonl
